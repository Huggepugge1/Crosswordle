class Cell:
    def __init__(self, position: tuple[int, int], state: bool, words: tuple[str, str], current_content: str, solution: str):
        self.position = position
        self.state = state
        self.words = words
        self.current_content = current_content
        self.solution = solution

def make_cells_from_user_state(user_state):
    pass

